version: "3.8"
services:
    db:
      image: postgres
      ports:
        - "5432:5432"
      environment:
        - POSTGRES_PASSWORD=chang123
        - POSTGRES_USER=ching
        - POSTGRES_DB=playground
